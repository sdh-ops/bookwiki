# 이 파일을 .env.local 로 복사해서 값을 채운다:  cp .env.example .env.local
# .env.local 은 절대 커밋하지 않는다(.gitignore 가 막는다).
#
# 아래 두 값은 원래 브라우저에 공개되는 키다 — 운영자에게 받아 넣는다.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
#
# 그 밖의 키(서비스 키·AI API 키 등)는 팀원 작업에 필요 없다. 없어도 게시판·게임은 돈다.
